export interface Division {
    x: number,
    y: number,
    width: number,
    height: number,
    id: string,
    isVisible?: boolean,
}

export interface Line {
    x: number,
    y: number,
    width?: number,
    height?: number,
}
  